{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "certinel.serviceAccountName" . }}
  labels:
    {{- include "certinel.labels" . | nindent 4 }}
{{- end -}}
